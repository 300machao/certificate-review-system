# 外检计量证书智能审核系统协作规则

## 作用域

本文件适用于项目根目录及其全部子目录。更深层的 `AGENTS.md` 只能补充更具体的约束，不得放宽本文件的数据、审计、模型费用、网络和部署红线。

本项目是企业接收侧的本机业务系统。任何任务都必须先区分只读预检、获批实施和验收；没有明确授权时，只允许读取源码、配置和脱敏材料，不得改变正式业务状态。

## 不可突破的红线

- 企业角色是外检计量证书接收和审核方。
- 永不直接修改、清理或提交正式data。
- 测试使用临时数据并禁用真实模型。
- 未经授权不产生模型费用、不迁移真实数据库、不部署。
- 缺少冻结台账、关键字段或权威证据时保持UNCERTAIN，绝不自动通过。
- 模型只能解释证据，不能补造证据。
- 未接官方验真时真实性始终是UNVERIFIED。
- 不得新增质检数智平台写回或公网监听。
- 人工决定不得覆盖机器原始结果，必须保留版本和审计记录。
- 数据库变更必须提供迁移测试、旧库兼容、备份和回滚。
- 完成标准必须包含浏览器可见业务路径验证。

## 正式数据边界

- `data/` 整体视为正式数据，包括 SQLite 主文件及 WAL/SHM、原件对象、验证结果、运行标记、日志、模型设置和 DPAPI 密文。
- 根目录的 `acceptance-result.*`、`validation/` 以及其他带有真实或未经确认业务信息的材料，除非完成逐字段脱敏审查，否则不得加入 Git。
- 不得用 Git checkout、reset、clean、stash、worktree 或脚本清理来恢复、移动或删除正式数据；Git 回滚只适用于已纳入版本管理的源码和文档。
- DPAPI 密文只在加密它的 Windows 用户上下文中可解密。不得提交密文，也不得把复制密文当作跨账户凭据迁移方案。
- 在兼容迁移完成并通过验收前，不得移动或重命名项目根目录或 `data/`。schema v2 中存在对象绝对路径契约，详见 `docs/decisions/0002-absolute-path-migration-gate.md`。

## 禁止的探测和批处理方式

- 禁止把导入正式 `app.api` 当作健康探测。导入会构造服务、打开并初始化数据库，还可能执行中断任务恢复。不得在正式数据上下文运行 `python -c "import app.api"` 或等价命令。
- 禁止在没有单独书面授权、费用确认、目标批次确认和回滚方案时运行 `scripts/backfill_text_model_reviews.py` 或任何 backfill、retry、批量导入脚本。
- 禁止为了“确认可用”而调用模型健康检查的写接口、连接测试或真实模型。只读源码检查不能替代浏览器验收，浏览器验收也必须使用临时数据和禁用模型。
- 禁止启动非回环监听。允许的监听地址仅为 `127.0.0.1`、`::1` 或等价的 `localhost`；不得使用 `0.0.0.0`、局域网地址或公网地址。

## 安全开发流程

1. 先读取本文件、相关 ADR、变更模板和当前 Git 状态。
2. 明确本次目标、允许修改的文件、数据边界、模型费用边界、迁移影响和验收方法。
3. 复杂或高风险变更使用独立分支和 Git worktree；一个任务只处理一个可审查结果。
4. 数据库变更先在旧库副本和合成临时库上实现迁移测试；不得对正式库试迁移。
5. 实现后先做离线自动测试，再做仅回环、临时数据、禁用模型的浏览器可见业务路径验证。
6. 提交前检查暂存清单，证明没有 `data/`、证书原件、密钥、日志或未经脱敏材料。
7. 报告实际完成项、未完成项、测试证据、浏览器证据、Git 状态和代码/数据各自的回滚方法。

## 安全测试命令

只可在已建立并固定依赖的项目虚拟环境中运行。PowerShell 会话必须先设置独立临时数据目录并禁用模型：

```powershell
$env:CERT_MODEL_MODE = "disabled"
$env:CERT_DATA_DIR = Join-Path ([System.IO.Path]::GetTempPath()) ("certificate-review-tests-" + [guid]::NewGuid().ToString("N"))
& .\.venv\Scripts\python.exe -m pytest -q
node --check .\static\app.js
```

现有 `tests/conftest.py` 还会在导入应用前再次指定临时目录和禁用模型，这是必须保留的第二道门禁。测试前后都应核对正式 `data/` 的文件数量、总字节和关键文件时间戳未发生变化。

浏览器可见验收必须在新的临时数据目录中启动，明确设置 `CERT_MODEL_MODE=disabled`，只绑定空闲的回环端口，并使用合成或已确认脱敏的夹具。不得用正式 `data/` 做验收演示。

## 完成定义

仅有代码修改、pytest 通过或 API 响应不等于完成。完成至少要求：范围内文件审查、离线测试、前端语法检查、仅回环监听检查、浏览器可见核心业务路径、正式数据未变化证明、敏感文件未暂存证明，以及可执行的 Git 回滚和数据恢复说明。任何一项未完成必须明确标为未完成，不得以推测代替证据。
