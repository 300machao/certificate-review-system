# 变更：<简短标题>

## 元数据

- 状态：草案 / 已批准 / 实施中 / 已验证 / 已回滚
- 负责人：
- 创建日期：
- 目标分支或 worktree：
- 基线提交：
- 关联 ADR：
- 授权引用：

## 业务目标

从企业接收和审核外检计量证书的视角，说明本次要解决的问题、用户可见结果和明确不解决的问题。不得在此粘贴真实业务编号、证书名、密钥、完整路径清单或未经脱敏证据。

## 允许范围

- 允许修改的文件：
- 允许新增的文件：
- 明确禁止触碰的正式数据/接口：
- 是否允许模型调用及费用上限：默认不允许
- 是否允许 schema 迁移：默认不允许
- 是否允许部署、远程或推送：默认不允许

## 现状证据

- 当前行为：
- 代码/配置证据：
- 浏览器可见证据：
- 已确认事实：
- 未确认假设：

## 设计

- 拟议变化：
- 保持不变的行为：
- `UNCERTAIN` / `UNVERIFIED` 门禁影响：
- 原始机器结果和人工版本保留方式：
- 数据、审计、隐私和网络影响：
- 版本兼容影响：

## 数据库迁移门禁（如适用）

- 旧 schema 版本：
- 新 schema 版本：
- 正式备份验证引用：
- 迁移只在副本运行的证明：
- 旧库兼容测试：
- 幂等/重复打开测试：
- 审计链与业务完整性：
- 对象路径与哈希验证：
- 回滚和恢复：

## 实施任务

- [ ] 最小代码/文档变化
- [ ] 临时数据夹具
- [ ] 自动测试
- [ ] 前端语法检查
- [ ] 仅回环监听检查
- [ ] 浏览器可见业务路径
- [ ] 正式 `data/` 未变化证明
- [ ] staged 文件敏感信息检查
- [ ] 文档、ADR 和回滚更新

## 验收标准

### 离线自动验证

- 命令：
- 预期：
- 实际：

### 浏览器可见验证

- 临时数据目录：受保护记录，不提交 Git
- 模型模式：必须为 `disabled`
- 监听：仅回环
- 导入/进度：
- 证据/原件预览：
- `UNCERTAIN` 与 `UNVERIFIED`：
- 人工版本与机器原始结果：
- 导出：

### 安全验证

- 正式数据前后清单一致：
- 无模型费用：
- 无平台写回：
- 无非回环监听：
- Git 未包含正式数据、日志、密钥或未经脱敏材料：

## 风险与缓解

| 风险 | 触发条件 | 缓解/停止门禁 |
|---|---|---|
|  |  |  |

## 回滚

- 代码回滚提交/标签：
- 数据恢复点：
- 触发条件：
- 回滚步骤：
- 回滚后浏览器验证：

## 最终结果

- 已完成：
- 未完成：
- 测试与浏览器证据：
- Git 状态：
- 数据状态：
- 后续工作：
